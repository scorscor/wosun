server {
    listen 80;
    server_name wosun.scor.vip;

    # 日志
    access_log /var/log/nginx/wosun-website-access.log;
    error_log /var/log/nginx/wosun-website-error.log;

    # 反向代理到Node.js应用
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # 静态文件缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
        proxy_pass http://localhost:3000;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS配置 (使用Let's Encrypt证书)
# server {
#     listen 443 ssl http2;
#     server_name wosun.scor.vip;
#
#     ssl_certificate /etc/letsencrypt/live/wosun.scor.vip/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/wosun.scor.vip/privkey.pem;
#
#     # SSL配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
#
# # HTTP重定向到HTTPS
# server {
#     listen 80;
#     server_name wosun.scor.vip;
#     return 301 https://$server_name$request_uri;
# }